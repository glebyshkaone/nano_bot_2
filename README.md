# nano-bot (nano-banana Telegram bot)

Минималистичный Telegram-бот на базе модели **google/nano-banana-pro** (Replicate).

## Функционал

- `/start` — приветствие и кнопка «🎛 Настройки генерации».
- `/menu` — открыть текущие настройки.
- `/help` — краткая инструкция.
- Инлайн-кнопки:
  - Соотношение сторон: 1:1, 4:3, 16:9, 9:16
  - Разрешение: 1K / 2K / 4K
  - Формат вывода: png / jpg
  - Уровень safety-фильтра.
- Любой текстовый промт → генерация картинки с учётом текущих настроек.

## Запуск локально

1. Установить зависимости:

   ```bash
   pip install -r requirements.txt
   ```

2. Создать `.env` на основе `.env.example` и указать:

   ```env
   TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
   REPLICATE_API_TOKEN=твой_токен_от_Replicate
   ```

3. Запустить:

   ```bash
   python bot.py
   ```

## Деплой на Railway

- Залей проект на GitHub.
- В Railway укажи переменные:
  - `TELEGRAM_BOT_TOKEN`
  - `REPLICATE_API_TOKEN`
- Команда запуска: `python bot.py`.

В логах бот выводит префикс токена Replicate и его длину, чтобы можно было проверить,
что Railway подхватил корректное значение.
